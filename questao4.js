function calculateCircumference(radius){
    return Math.PI * (radius * radius);
}

console.log(Math.PI);

console.log(calculateCircumference(10));

console.log(Math.round(calculateCircumference(10)));

console.log(Math.ceil(calculateCircumference(10)));

console.log(Math.floor(calculateCircumference(10)));